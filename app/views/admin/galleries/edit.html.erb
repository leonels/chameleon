<h1>Editing gallery</h1>

<p><%= link_to 'Add a photo', new_admin_gallery_photo_path(@gallery) %></p>

<% if params[:page_id] %>
  <%= render 'nested_form' %>  
<% else %>
  <%= render 'form' %>
<% end %>
